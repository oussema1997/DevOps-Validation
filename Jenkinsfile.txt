pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                script {
                    def mvnHome = tool name: 'M2_HOME', type: 'Maven'
                    sh "${mvnHome}/bin/mvn clean install"
                }
            }
        }
    }
}