pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                script {
                    def mvnHome = ${M2_HOME}
                    sh "${mvnHome}/bin/mvn clean install"
                }
            }
        }
    }
}